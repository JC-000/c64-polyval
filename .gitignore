# Pattern, not a list. `build/` and `build-knobcheck/` were enumerated by
# name and `build-short/` was not, so six build artifacts sat TRACKED in git
# for a release cycle (issue #99) -- including a polyval-short.a predating the
# v0.11.0 member-isolation fix. c64-x25519#145 is the same drift in a sibling
# repo, three times over. A pattern needs no third edit.
/build*/
__pycache__/
*.pyc
.serena/

# ca65 language-server index cache (Serena / ca65-ls). Machine-local,
# regenerated on demand.
.ca65-ls/

# Per-developer Claude Code settings. settings.local.json in particular is
# explicitly personal.
# Everything under .claude/ is local EXCEPT the reviewer definition that
# CLAUDE.md's Working standard names. Note the shape: an excluded DIRECTORY is
# never descended into, so `.claude/` + `!.claude/agents/**` does not work --
# the contents must be excluded instead, then re-included level by level.
.claude/*
!.claude/agents/
.claude/agents/*
!.claude/agents/adversarial-reviewer.md
